apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: misra-backend
  namespace: misra-platform
  labels:
    release: prometheus
spec:
  selector:
    matchLabels:
      app: misra-backend
  endpoints:
    - port: http
      path: /metrics
      interval: 30s
